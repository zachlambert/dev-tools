services:
  rust-dev:
    network: host
    ipc: host
    build:
      context: .
      args:
        USER: ${USER}
        UID: ${UID}
        GID: ${GID}
        SHELL: ${SHELL}
    image: rust-dev
    container_name: rust-dev
    user: ${USER}
    command: "sleep infinity"
