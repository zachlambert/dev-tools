services:
  #   rust-dev-tools:
  #     network_mode: host
  #     ipc: host
  #     build:
  #       context: .
  #       args:
  #         DEV_IMAGE: rust-dev
  #         USER: ${USER}
  #         UID: ${UID}
  #         GID: ${GID}
  #         SHELL: ${SHELL}
  #     image: rust-dev-tools
  #     container_name: rust-dev-tools
  #     user: ${USER}
  #     command: "sleep infinity"
  #
  platform-io-dev-files:
    extends:
      file: /home/zachlambert/ros2_ws/src/platform_io/docker/docker-compose.yaml
      service: dev
    image: platform-io-dev-files:${DOCKER_TAG:-latest}
    container_name: platform-io-dev-files
    build:
      context: .
      dockerfile: Dockerfile
      target: dev
      args:
        USERNAME: ${USERNAME}
        DEV_IMAGE: platform-io-dev
        GIT_NAME: "Zach Lambert"
        GIT_EMAIL: "zl25@beam.global"
    volumes:
      - ./nvim:/home/user/.config/nvim
      - ./zsh:/home/user/.config/zsh
